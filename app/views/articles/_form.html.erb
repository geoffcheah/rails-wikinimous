<%= form_for(article) do |a| %>
  <div class="form-group">
    <%= a.label :title %>
    <%= a.text_field :title %>
  </div>
  <div class="form-group">
    <%= a.label :content %>
    <%= a.text_field :content %>
  </div>
  <div class="btn">
    <%= a.submit %>
  </div>
<% end %>
